import * as React from 'react';

/**
 * Represents a component that displays "Forbidden" message.
 *
 * @returns The rendered component.
 */
export const Forbidden = () => <p>Forbidden</p>;

export default Forbidden;
