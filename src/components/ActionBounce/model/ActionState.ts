export enum ActionState {
    Initial = 'initial-state',
    Active = 'active-state',
    Abort = 'abort-state',
    Succeed = 'succeed-state',
    Closed = 'closed-state',
}

export default ActionState;
