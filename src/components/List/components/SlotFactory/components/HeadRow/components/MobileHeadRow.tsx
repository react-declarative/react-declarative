import * as React from 'react';

import { IHeadRowSlot } from '../../../../../slots/HeadRowSlot';

/**
 * MobileHeadRow component
 *
 * This component represents the mobile version of the head row in a table.
 *
 * @component
 * @example
 * <MobileHeadRow />
 */
export const MobileHeadRow: React.FC<IHeadRowSlot> = () => {
    return <></>;
};

export default MobileHeadRow;
