# MasterDetailMode

```ts
export enum MasterDetailMode {
    Paper = "paper",
    Outline = "outline",
    Card = "card",
    Tabs = "tabs"
}
```

Represents the available modes for a master-detail view.
